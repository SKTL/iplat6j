package com.baosight.imc.xb.ai.service;

import com.baosight.iplat4j.core.ei.EiInfo;
import com.baosight.iplat4j.core.service.impl.ServiceBase;

public class ServiceXBAI01 extends ServiceBase {
    @Override
    public EiInfo initLoad(EiInfo inInfo) {
        return super.initLoad(inInfo);
    }

    @Override
    public EiInfo query(EiInfo inInfo) {
        return super.query(inInfo);
    }

    @Override
    public EiInfo delete(EiInfo inInfo) {
        return super.delete(inInfo);
    }

    @Override
    public EiInfo update(EiInfo inInfo) {
        return super.update(inInfo);
    }

    @Override
    public EiInfo insert(EiInfo inInfo) {
        return super.insert(inInfo);
    }

    public EiInfo addHistorySession(EiInfo inInfo) {
        EiInfo outInfo = new EiInfo();

        return outInfo;
    }
}
